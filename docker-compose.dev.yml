# Dev override: expose Postgres to localhost so the local daemon can connect.
# Usage (via `make db`): docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d
services:
  virtad:
    profiles: [production]
  db:
    ports:
      - "127.0.0.1:5433:5432"
