{
  "name": "@virta/frontends",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["ui-kit", "feed-core", "web"],
  "scripts": {
    "lint:tokens": "node scripts/token-lint.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench:feed": "node bench/feed-bench.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "vitest": "^4.1.8"
  }
}
